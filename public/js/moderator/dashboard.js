document.addEventListener('DOMContentLoaded', function() {
    // No need to fetch data, it’s already passed from the server and rendered in the HTML
    // Function to update the dashboard (for demonstration purpose)
    function updateDashboard() {
        // In a real application, you can add more dynamic behaviors here if needed
    }

    updateDashboard();
});
